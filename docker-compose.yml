version: '3.8'

services:
  backend:
    build: .
    container_name: 3gpp-rag-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  frontend:
    build: .
    container_name: 3gpp-rag-frontend
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://backend:8000
    command: streamlit run frontend/streamlit_app.py --server.port=8501 --server.address=0.0.0.0
    depends_on:
      - backend
